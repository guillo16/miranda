console.log("Create file executed.");
challengeForm = document.getElementById("challenge-response");
challengeForm.innerText = "";
challengeForm.insertAdjacentHTML("beforeend", "Great!  We just sent a challenge to that user.");
